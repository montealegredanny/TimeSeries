#include "OLS.h"

std::vector<double> OLS::runRegression(std::vector<double> X, Matrix Z, bool add_intercept)
{
	// create the matrices we need for the regression 
	// we need to solve the equation Z * Z.t beta = Z * X

	return std::vector<double> ();
}